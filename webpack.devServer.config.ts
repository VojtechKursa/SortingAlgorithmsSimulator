import { Port } from "webpack-dev-server";

// The hostname on which the development server will be started. Change to undefined to listen on all network interfaces.
export const host: string | undefined = 'localhost';

// The port on which the development server will be started. Change to undefined to let the server pick a suitable port.
export const port: Port | undefined = 5000;
